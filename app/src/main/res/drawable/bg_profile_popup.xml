<?xml version="1.0" encoding="utf-8"?>
<!-- Card behind the account dropdown anchored under the home header avatar -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/auth_card_bg" />
    <corners android:radius="16dp" />
    <stroke
        android:width="1dp"
        android:color="@color/brown_accent" />
</shape>
